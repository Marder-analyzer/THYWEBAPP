
<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <title>Rezervasyon Detayları</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-color: #f8f9fa;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            margin: 0;
        }

        .navbar {
            background-color: #910e0e;
            padding: 10px;
            display: flex;
            align-items: center;
            justify-content: space-between;
            color: white;
        }

            .navbar h1 {
                margin: 0;
                font-size: 20px;
                font-weight: bold;
                color: white;
            }

            .navbar .nav-links {
                display: flex;
                justify-content: center;
                width: 100%;
                position: relative;
            }

                .navbar .nav-links a {
                    color: white;
                    text-decoration: none;
                    font-weight: bold;
                    padding: 5px 15px;
                    border-radius: 5px;
                }

                    .navbar .nav-links a.active {
                        background-color: white;
                        color: #910e0e;
                    }

            .navbar .nav-links {
                position: absolute;
                left: 50%;
                transform: translateX(-50%);
            }

      

        .container {
            background: white;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            padding: 30px;
            max-width: 800px;
            margin: auto;
            margin-top: 50px;
        }

        h2 {
            color: #910e0e;
            text-align: center;
            margin-bottom: 20px;
        }

        .detail-group {
            margin-bottom: 20px;
        }

            .detail-group label {
                font-weight: bold;
                color: #495057;
            }

            .detail-group p {
                margin: 5px 0;
                font-size: 16px;
            }

        .btn-primary {
            background-color: #910e0e;
            border-color: #910e0e;
            font-size: 18px;
            width: 100%;
            margin-top: 20px;
        }

            .btn-primary:hover {
                background-color: #720c0c;
                border-color: #5a0a0a;
            }
    </style>
</head>
<body>
    <div class="navbar">
        <h1>Türk Hava Yolları</h1>
        <div class="nav-links">
            <a>Uçuş Seçimi</a>
            <a class="active">Yolcu Bilgileri</a>
            <a>Ödeme</a>
        </div>
    </div>

    <div class="container">
        @if (TempData["Success"] != null)
        {
            <div class="alert alert-success">@TempData["Success"]</div>
        }
        @if (TempData["Error"] != null)
        {
            <div class="alert alert-danger">@TempData["Error"]</div>
        }
        <h2>Rezervasyon Detaylarınızı Onaylayın</h2>
        <p><strong>Kalkış Havaalanı: </strong> @ViewBag.DepartureAirportName</p>
        <p><strong>Varış Havaalanı: </strong>@ViewBag.ArrivalAirportName</p>
        <p><strong>Kalkış Tarihi ve Saati: </strong>@ViewBag.DepartureDate</p>
        
        <p><strong>Satın Alınan Paket:</strong>@ViewBag.PackageName</p>
        <p><strong>Bagaj Kilo Hakkı: </strong>@ViewBag.BaggageAllowance Kg</p>
        <p><strong>Toplam Ücret: </strong>@ViewBag.PackagePrice</p>
        <p><strong>Member Üyelikli (İndirimli) Ücret: </strong>@ViewBag.DiscountedPrice</p>

        <h3>İletişim Bilgileri</h3>
        <p><strong>E-posta: </strong>@ViewBag.PassengerEmail</p>
        <p><strong>Telefon Numarası: </strong>@ViewBag.PassengerPhoneNumber</p>

        

        <form method="post" action="/Reservation/CreateReservation">
            <button type="submit" class="btn btn-primary">Onayla</button>
        </form>
        <div style="position: fixed; bottom: 20px; left: 20px;">
            <a href="@Url.Action("Index", "Home")" class="btn btn-primary" style="background-color: #007bff; color: white; text-decoration: none; padding: 10px 20px; border-radius: 5px;">Anasayfaya Dön</a>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
